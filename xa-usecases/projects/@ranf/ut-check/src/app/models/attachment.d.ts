// TODO: this will be modified. Attachments would be handled differently
// Further discussion is needed with the backend to clarify the way of handling of attachments
export interface Attachment {
    Data: string;
    Name: string;
    Type: string;
}